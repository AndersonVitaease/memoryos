{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "plan": {
      "type": "string",
      "description": "Plano do usu\u00e1rio",
      "enum": [
        "free",
        "pro",
        "business"
      ],
      "default": "free"
    }
  },
  "required": [
    "role"
  ]
}